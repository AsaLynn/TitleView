# MyTitleView
一个自定义标题控件.
```
implementation 'com.zxn.titleview:TitleView:1.0.9'
implementation 'com.zxn.titleview:TitleView:1.0.8'
implementation 'com.zxn.titleview:TitleView:1.0.7'
```

标签

```
git tag -a v1.0.9 -m 'TitleView:1.0.9:1,增加标题背景属性.'
git push origin v1.0.9
git tag
```

```
git tag -a v1.0.8 -m 'TitleView:1.0.8:1,增加标题字体属性.'
git push origin v1.0.8
git tag
```

日志调试:
```
<attr name="titleTextSize" format="dimension" />
The given artifact contains a string literal with a package reference 'android.support.v4.content'
 that cannot be safely rewritten. 
 Libraries using reflection such as annotation processors need to be updated manually to add support for androidx.
```
